#include<stdio.h>
#include<string.h>
main()
{
	char name[20];
	int age;
}
void show(struct student s);
main()
{
	struct student s1={1,21};
	show(s1);
};
void show(show struct s)
{
	printf("is %d",s.id);
	printf("age is %d",s.age);
	s2.age=32;
	s2.id=50;
	return s2;
}
